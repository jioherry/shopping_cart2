<h1>Order #<%= @order.id %></h1>

訂單時間：<%= @order.created_at.strftime('%Y-%m-%d %H:%M')%><br>
收件人：<%= @order.name %><br>
信箱：<%= @order.email %><br>
電話：<%= @order.phone %><br>
地址：<%= @order.address %><br>

<br>
<table>
  <tr>
    <th>訂購商品</th>
    <th>數量</th>
    <th>單價</th>
    <th>小計</th>
  </tr>
  <% @order.order_items.each do |item| %>
    <tr>
      <td><%= item.product.name %></td>
      <td><%= item.quantity %></td>
      <td><%= item.price %></td>
      <td><%= item.quantity * item.price %></td>
    </tr>
  <% end %>
</table>
<br>
<strong>總計: $ <%= @order.amount %></strong>